# ialdev-annotations

Python import path: **`iad.annotations`** — issue tables, CSV I/O, and annotation visualization built on `iad.core`, `iad.dataman`, and `iad.vis`.

See the repository root `README.md` for workspace install and publishing (`flit build` / `flit publish` in this directory).
