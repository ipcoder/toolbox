# ialdev-engines

Python import path: **`iad.engines`** — engine registry, catalogs, and labeled I/O types built on `iad.core` and `iad.dataman`.

See the repository root `README.md` for workspace install and publishing (`flit build` / `flit publish` in this directory).
